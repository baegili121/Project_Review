# PROJECT_SPEC: 전형별 채용공고 탐색기

이 앱은 여러 채용공고를 비교하거나 합쳐 보여주는 서비스가 아닙니다.

## 핵심 원칙

- 후보 자료는 여러 개 찾을 수 있습니다.
- 분석 대상은 반드시 `selectedSource` 1개입니다.
- 최종 분석 결과는 `job` 1개 또는 `null`만 허용합니다.
- `job` 배열은 오류로 처리하고 첫 번째 항목을 사용하지 않습니다.
- `selectedSource`가 `null`이면 `job`도 반드시 `null`입니다.
- `job.sourceUrl`은 반드시 `selectedSource.url`과 같아야 합니다.
- 다른 공고, 다른 연도, 다른 전형, 다른 직무의 내용을 섞지 않습니다.
- 확인되지 않은 정보는 `확인 불가`로 표시합니다.
- Gemini 또는 검색 환경에서 실제 웹/PDF/이미지 접근이 불가능하면 임의 결과를 만들지 않습니다.
- 고졸 세부 유형은 전형 유형이 `고졸`일 때만 노출합니다.
- 실제 서비스 배포 시에는 API 키를 클라이언트에 저장하면 안 됩니다. 서버 환경변수 또는 서버리스 프록시를 통해 안전하게 호출해야 합니다.
